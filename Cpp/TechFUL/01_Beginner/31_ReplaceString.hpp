#include <iostream>
#include <string>

void solve()
{
    using namespace std;
    string source, find, replace;
    getline(cin, source);
    getline(cin, find);
    getline(cin, replace);

    string out = "";

    for (int i = 0; i < source.size(); ++i)
    {
        for (int j = 0; j < find.size(); ++j)
        {

        }
    }
}